# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8080/api

# Environment
VITE_NODE_ENV=development

# App Configuration
VITE_APP_NAME=SkyBook
VITE_APP_VERSION=1.0.0

# Analytics (Optional)
# VITE_GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID
# VITE_HOTJAR_ID=HOTJAR_SITE_ID

# Feature Flags (Optional)
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_LOYALTY_PROGRAM=true
VITE_ENABLE_CHAT_SUPPORT=false

# API Endpoints Configuration
# These match your Spring Boot backend structure
# /api/auth/login - POST (email, password)
# /api/auth/register - POST (name, email, password)  
# /api/auth/me - GET (requires JWT token)
# /api/flights/search - GET (departure, arrival, date, passengers)
# /api/flights/{id} - GET (flight details)
# /api/bookings - POST (create booking)
# /api/bookings - GET (user bookings, requires JWT)
# /api/users/profile - GET/PUT (user profile management)

# JWT Configuration (handled automatically by the frontend)
# Tokens are stored in localStorage and automatically included in API requests
# Token expiration is handled with automatic logout
